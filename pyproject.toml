[project]
name = "ranked-choice-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "discord>=2.3.2",
    "pyrankvote>=2.0.6",
    "python-dotenv>=1.2.2",
]
